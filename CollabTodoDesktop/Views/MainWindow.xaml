<Window x:Class="CollabTodoDesktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CollabTodoDesktop.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="640" Width="1024"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <DockPanel>
        <!-- 메뉴 -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="도구">
                <MenuItem Header="AI 요약 테스트" Click="OnAiSummaryTest_Click"/>
            </MenuItem>
        </Menu>

        <!-- 상태바 -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Grid.Column="0">
                <TextBlock Text="애플리케이션이 정상적으로 시작되었습니다."/>
            </StatusBarItem>
            <Separator Grid.Column="1"/>
            <StatusBarItem Grid.Column="2">
                <StackPanel Orientation="Horizontal">
                    <Label Content="{Binding ConnectionStatus}" Margin="5,0"/>
                    <Separator VerticalAlignment="Center" Margin="5,0"/>
                    <Label Content="{Binding LastSyncTimeText}" Margin="5,0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <!-- 메인 컨텐츠 -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 중앙 컨텐츠 -->
            <Label Grid.Column="0" 
                   Content="Collab To-Do Desktop" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   FontSize="24"/>

            <!-- 대시보드 (오른쪽 도크) -->
            <DockPanel Grid.Column="1" Width="250" Background="WhiteSmoke">
                <Label DockPanel.Dock="Top" 
                       Content="내 작업 요약" 
                       FontWeight="Bold" 
                       Padding="10,5"
                       Background="LightGray"/>
                <ListBox ItemsSource="{Binding DashboardItems}" 
                         BorderThickness="0"
                         Padding="10,5"/>
            </DockPanel>
        </Grid>
    </DockPanel>
</Window>

